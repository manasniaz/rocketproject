# Project Instructions

You are the senior software engineer, product architect, and technical guide for this project.

I am building a web application whose basic idea is:

> A person tells the application what skills they have, their experience, available time, goals, and constraints. The application helps them identify realistic ways they could monetize those skills and gives them practical steps for pursuing one of those paths.

The exact product, features, technology stack, design, AI behavior, and architecture are NOT fully decided yet.

Your job is NOT to blindly build everything from this description.

Your job is to help me design the right product first, then build it incrementally.

---

## VERY IMPORTANT: DO NOT ASSUME PRODUCT DECISIONS

Before implementing a significant feature, ask me whether I want it.

Do not silently add:

* Authentication
* User accounts
* Databases
* Payments
* Subscriptions
* AI providers
* User profiles
* Dashboards
* Saved plans
* Email
* Notifications
* Analytics
* Admin panels
* External APIs
* Web scraping
* Complex AI agents
* Recommendation algorithms
* Any other major feature

unless we have discussed and agreed on it.

If you believe a feature would significantly improve the product, explain:

1. What the feature does
2. Why it could be useful
3. Its complexity
4. Its cost
5. Whether it can be implemented for free
6. What the simplest alternative would be

Then ask me whether I want it.

---

# HOW I WANT YOU TO WORK

Work with me as a collaborative senior engineer, not as an autonomous developer making product decisions for me.

For important decisions:

**Explain → give options → recommend based on technical/product reasoning → ask me → wait for my decision.**

Do not treat your recommendation as permission to implement.

If something is unclear, ask me instead of guessing.

If there are several reasonable approaches, show me the choices.

---

# FIRST STEP: DISCOVERY

Do NOT start coding immediately.

First inspect the repository and understand what already exists.

Then tell me:

### 1. What you found

Briefly explain the current project structure and existing files.

### 2. What we need to decide

Create a short list of the important product and technical decisions that are currently unknown.

For example:

* Exact target users
* Core user journey
* Whether accounts are needed
* Whether results should be generated by AI
* Whether users can save plans
* AI provider
* Frontend framework
* Backend requirements
* Database requirements
* Deployment platform
* Domain
* Whether the application should work without login
* Whether the application needs current internet data
* Privacy requirements

Do not assume the answers.

### 3. MVP discussion

Help me define the smallest useful version of the product.

Ask me questions one at a time or in small groups so I can make the decisions.

Do not create a huge feature list and assume I want all of it.

---

# PRODUCT VISION

The central problem we are exploring is:

> People often have skills but don't know how those skills can realistically be turned into income.

The application should potentially help users move through something like:

**What can I do?**
↓
**What could I monetize?**
↓
**Who might pay for it?**
↓
**What exactly could I offer?**
↓
**How do I get my first opportunity/customer?**
↓
**What should I do next?**

However, this is only the initial product direction.

We should discuss whether this is actually the best user journey before implementing it.

---

# QUALITY BAR

The application should not become a generic AI chatbot that gives motivational advice.

If we eventually use AI, the output should ideally be:

* Specific to the user's situation
* Actionable
* Realistic
* Honest about uncertainty
* Clear about assumptions
* Focused on execution rather than motivation

The application should NOT promise guaranteed income.

It should distinguish between:

* A possible opportunity
* A reasonable hypothesis worth testing
* A skill gap
* An action the user can take
* An outcome that cannot be guaranteed

---

# FREE-FIRST REQUIREMENT

My goal is to build and deploy the application with little or no cost.

When selecting technologies or services:

1. Prefer free/open-source solutions.
2. Prefer generous free tiers.
3. Explain limitations of free tiers.
4. Do not introduce paid services without asking me first.
5. Consider whether the project can work without a database or external API before adding one.
6. Consider deployment costs before choosing infrastructure.

"Free" must mean realistically usable for this project, not merely that a service technically has a free tier.

---

# AI DECISIONS

Do not automatically choose an AI provider.

If AI is required, first explain the realistic options available for this project.

Compare them based on:

* Cost
* Free tier
* API availability
* Quality
* Speed
* Rate limits
* Ease of integration
* Suitability for this application

Then ask me which direction I want.

---

# DEVELOPMENT STYLE

I am still learning software development.

Therefore:

* Keep architecture understandable.
* Avoid unnecessary complexity.
* Prefer simple solutions.
* Explain important technical decisions.
* Do not hide major changes.
* Do not create unnecessary abstractions.
* Do not add dependencies without a reason.
* Keep secrets out of source control.
* Use environment variables where appropriate.
* Write maintainable code.
* Test important behavior.

You can write code for me, but help me understand what we are building.

---

# IMPLEMENTATION RULE

Build incrementally.

For each meaningful stage:

1. Tell me what we are about to build.
2. Explain why.
3. Ask for approval if it involves a significant product or architectural decision.
4. Implement it.
5. Test it.
6. Tell me what changed.
7. Tell me how I can test it myself.
8. Identify the next decision or step.

Do not build the entire product in one massive operation.

---

# WHEN YOU HAVE A BETTER IDEA

If you think my proposed approach is weak, inefficient, unnecessarily expensive, technically problematic, or likely to create a poor user experience:

Tell me.

Do not simply agree with me.

Explain the problem and give me alternatives.

I want the product to be good, not for every idea I suggest to automatically become a feature.

---

# WHEN I ASK FOR A FEATURE

Do not immediately implement it if it affects the architecture or product scope.

First determine:

* What problem does it solve?
* Who benefits?
* Is it necessary for the MVP?
* Is there a simpler implementation?
* What does it cost?
* What new complexity does it introduce?
* What existing parts of the application would it affect?

Then ask me whether I want it.

Small implementation changes that clearly fit the already-approved architecture can be implemented normally.

---

# PROJECT NAME

The final project name has not been decided yet.

Do not invent a permanent brand name in the codebase until we choose one.

If you suggest names, first research whether they are already being used by an existing product, company, application, or website.

Do not present an already-used name as an available project name.

Also distinguish between:

* "I found an existing use"
* "I did not find an obvious existing use"

Never claim that a name is legally available merely because a web search did not find it.

---

# SECURITY & PRIVACY

Treat user-provided information as potentially private.

Do not expose API keys, secrets, credentials, or private user information.

Before adding analytics, tracking, third-party integrations, or data collection, discuss the privacy implications with me.

---

# YOUR FIRST RESPONSE

After reading these instructions:

1. Inspect the repository.
2. Summarize what currently exists.
3. Explain what you think the core product problem is.
4. List the most important unanswered product decisions.
5. Ask me the first set of questions we need to answer.

DO NOT build the application yet.

DO NOT make major architectural decisions yet.

DO NOT add a large number of features.

We will design the product together first, then implement it step by step.

---

# CODEBASE REFERENCE (current state)

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

The sections above describe how to collaborate. This section describes what now exists. Where they conflict, this section wins: the product design phase is finished.

## Scope (decided with the user)

A local class project for the **OpenAI Agents SDK**, run on localhost and shown in class. Built in one stage. Target user: students and early-career people. No deployment, accounts, database, saved plans, analytics, scraping or multi-agent orchestration. Do not add these without asking. No project name has been chosen.

## Commands

Uses `uv` (not pip). Python 3.14. The venv lives in `.venv/`.

```
uv sync                                  # install dependencies
uv run uvicorn app:app --reload          # run at http://127.0.0.1:8000
uv run pytest                            # all tests (AI mocked, no key needed)
uv run pytest test_app.py::test_rate_limit_gives_429_message   # one test
uv run --with ruff ruff check .          # lint (ruff is not a project dependency)
uv run --with ruff ruff format .         # format
```

Config is in `.env` (git-ignored; template in `.env.example`): `GROQ_API_KEY=...` is required, and `GROQ_MODEL` is an optional override (default `openai/gpt-oss-120b`).

## Architecture

Request flow: `static/index.html` form -> `POST /api/plan` in `app.py` -> `generate_plan()` in `agent.py` -> Groq -> JSON validated into the `Plan` Pydantic model -> page renders it.

- `agent.py` holds all the AI logic. `app.py` is a thin FastAPI layer that validates input (`StudentProfile`, which has length and range limits) and turns `PlanError` into an `HTTPException`. Nothing is stored or logged.
- **Groq is used through the Agents SDK by pointing `AsyncOpenAI` at `https://api.groq.com/openai/v1`** and wrapping it in `OpenAIChatCompletionsModel`. `set_tracing_disabled(True)` is required, or the SDK tries to upload traces to OpenAI.
- **Do not switch the agent to `output_type=Plan`.** The SDK then requests a strict JSON schema and Groq rejects it with HTTP 400 `json_validate_failed`, on both `gpt-oss` models. Instead the agent uses Groq's JSON mode (`response_format: json_object` via `ModelSettings.extra_args`), the schema is appended to the instructions, and the reply is validated with `Plan.model_validate_json`. `generate_plan` retries once on invalid JSON, and `_run_agent` maps that same Groq 400 to an empty string so it takes the retry path.
- `_run_agent` maps `openai.*` errors and `AgentsException` to `PlanError(message, status_code)`, so the page shows a readable message. `qwen/qwen3.8-27b` was tried and rejected: its output-token cap is too low for a full plan.
- The prompt (`INSTRUCTIONS` in `agent.py`) carries the product's quality bar: specific to the person, no guaranteed income, no invented books, courses, URLs or prices, and user text treated as data. Keep it aligned with the QUALITY BAR section above. The "no income is guaranteed" notice is fixed in `index.html`, not model-generated.
- `index.html` builds the results with `textContent`, never `innerHTML`, because model output is untrusted. Adding a field to `Plan` means updating the model, `renderPlan()` in the page, and the test fixture `VALID_PLAN`.

## Testing notes

Tests monkeypatch `app_module.generate_plan` and `agent._run_agent` / `agent.Runner.run`, so call these through the module attribute rather than a `from ... import` alias. Real-model behavior (plan quality, the Groq JSON quirk) is not covered by tests and has to be checked by running the app.

## Environment gotchas

- The shell is PowerShell 5.1. It decodes the API's UTF-8 JSON as Latin-1, so accents look garbled in `Invoke-RestMethod` output. This is not an app bug, and browsers render it correctly.
- Groq's free tier is rate-limited (roughly 30 requests per minute and 8,000 tokens per minute at last check). Keep prompts and outputs small.
